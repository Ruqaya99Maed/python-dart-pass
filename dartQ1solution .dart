main(){
  int hour = DateTime.now().hour;
  print(hour);
}
/* The variable is declared with a constant value const and the called function
is a variable (time function) we must delete the const and we define the variable hour by using var or int */
